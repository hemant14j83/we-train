<%= form_for(@category) do |f| %>
  <% if @category.errors.any? %>
    <div id="error_explanation">
      <h3><%= pluralize(@category.errors.count, "error") %> prohibited this from being saved:</h3>
      <ul>
      <% @category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="col-xs-12 col-md-12 col-sm-12">
    <div class="col-xs-0 col-md-3 col-sm-0"></div>
    <div class="col-xs-12 col-md-6 col-sm-12 form-horizontal">
      <div class="form-group col-md-12">
        <%= f.text_field :name,:placeholder=>"Category Name [Required]", :class=>'form-control' %>
      </div>
      <div class="form-group col-md-6">
        <%= f.submit "Submit", :class=>"btn btn-success btn-lg" %>
      </div>
    </div>
    <div class="col-xs-0 col-md-3 col-sm-0"></div>
  </div>
<% end %>