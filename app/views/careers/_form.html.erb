<%= form_for(@career) do |f| %>
  <% if @career.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@career.errors.count, "error") %> prohibited this career from being saved:</h2>

      <ul>
      <% @career.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="col-xs-12 col-md-12 col-sm-12">
    <div class="col-xs-0 col-md-4 col-sm-0"></div>
    <div class="col-xs-12 col-md-4 col-sm-12 form-horizontal">
      <div class="form-group col-md-12">
        <%= f.label :position %><br><%= f.text_field :position, :class=>'form-control' %>
      </div>
      <div class="form-group col-md-12">
        <%= f.label :department %><br><%= f.text_field :department, :class=>'form-control' %>
      </div>
      <div class="form-group col-md-12">
        <%= f.label :description %><br><%= f.text_area :description, :class=>'form-control' %>
      </div>
      <div class="form-group col-md-12">
        <%= f.label :status %><br><%= f.select :status,['Open','Closed'], :class=>'form-control' %>
      </div>
      <div class="actions">
        <%= f.submit "Post Jobs", :class=>"btn btn-success" %>
      </div>
    </div>
    <div class="col-xs-0 col-md-4 col-sm-0"></div>
  </div>
<% end %>